# src/ui/

UI-related code, views, panels, and platform-agnostic UI logic. Panels should include the `main_window`, `search_panel`, `graph_panel`, `favorites_panel`, `notes_panel`, and `project_panel` subfolders per the architecture document.