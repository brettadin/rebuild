# src/platform/

Platform-specific code (OS detection, filesystem adapter, logging integration, and launcher).

Phase 0 currently contains:
- `launcher.py` — entry point for launching the GUI app.
- `logging_adapter.py` — minimal logging setup utility.
