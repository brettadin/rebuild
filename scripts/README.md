# scripts/

Helper scripts for build, test, packaging, and sample data fetching. Example content:
- `fetch_sample_data.*` — download/update real sample subsets
- `run_tests.*` — run tests
- `build_app.*` — build binaries
- `package_installer.*` — create installers for Windows/macOS

Scripts should also be documented with usage examples and necessary environment variables.